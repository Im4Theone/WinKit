<UserControl x:Class="WinKit.UI.Views.SystemTools.InstalledAppsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:WinKit.UI.ViewModels.SystemTools"
             xmlns:models="clr-namespace:WinKit.SystemTools.Models;assembly=WinKit.SystemTools"
             d:DataContext="{d:DesignInstance vm:InstalledAppsViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008/design"
             mc:Ignorable="d" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <DockPanel>
        <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Apps.Count, StringFormat={}{0} applications}" Style="{StaticResource Text.Secondary}" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource Button.Secondary}" Padding="14,7"/>
        </Grid>

        <Border Style="{StaticResource Card}" Padding="0">
            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="560">
                <ListView ItemsSource="{Binding Apps}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Name" Width="280" DisplayMemberBinding="{Binding Name}"/>
                            <GridViewColumn Header="Publisher" Width="180" DisplayMemberBinding="{Binding Publisher}"/>
                            <GridViewColumn Header="Version" Width="100" DisplayMemberBinding="{Binding Version}"/>
                            <GridViewColumn Header="Size" Width="90">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate DataType="{x:Type models:InstalledApp}">
                                        <TextBlock Text="{Binding EstimatedSizeMb, StringFormat={}{0:0} MB}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="" Width="100">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate DataType="{x:Type models:InstalledApp}">
                                        <Button Content="Uninstall" Style="{StaticResource Button.Destructive}" Padding="10,4"
                                                FontSize="{DynamicResource Font.Size.Small}"
                                                Command="{Binding DataContext.UninstallCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                CommandParameter="{Binding}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </ScrollViewer>
        </Border>
    </DockPanel>
</UserControl>
